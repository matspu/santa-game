using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SnowGround : MonoBehaviour
{


    /*

    void Start(){
        Instantiate(snowGround, snowGround.transform.position, Quaternion.identity);
    }


    void Update(){
        public snowParent = GameObject.Find("Snow");
        GameObject end = snowParent.transform.GetChild(0).gameObject;
        float distance = Vector3.Distance(spawner.transform.position, end.transform.position);
        if(distance <= 10f){
            SpawnSnow();
            // remove the last one

        }
    }

    void SpawnSnow(){
        GameObject newSnowGround = Instantiate(snowGround, new Vector3(end.transform.position.x -10f, end.transform.position.y, end.transform.position.z + 30f), Quaternion.identity);
        Destroy(snowParent, 10);
    }
    */
}
